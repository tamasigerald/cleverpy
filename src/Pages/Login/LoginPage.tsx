import React, { FC } from 'react';

import ThemeButton from 'Components/ThemeButton';

const LoginPage: FC = () => {
    return (
        <div>
            Login page
            <ThemeButton />
        </div>
    );
};

export default LoginPage;
